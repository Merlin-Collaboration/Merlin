# MERLIN class library makefile
# Automatically generated on Tue Oct 27 11:45:00 GMT 2009
# -----------------------------------------------------------

OFILES= \
     ${OBJDIR}/ChannelServer.o \
     ${OBJDIR}/ElementRepository.o \
     ${OBJDIR}/StdChannelDefs.o \

catagory: ${OFILES}


# Explicit make rules for each *.cpp file identified.
# The object files (targets) are located in the directory
# designated by the macro OBJDIR
# -------------------------------------------------------

${OBJDIR}/ChannelServer.o:
	${CPP} -c ${INCPATH} ${CCFLAGS} -o $@ ChannelServer.cpp
${OBJDIR}/ElementRepository.o:
	${CPP} -c ${INCPATH} ${CCFLAGS} -o $@ ElementRepository.cpp
${OBJDIR}/StdChannelDefs.o:
	${CPP} -c ${INCPATH} ${CCFLAGS} -o $@ StdChannelDefs.cpp
